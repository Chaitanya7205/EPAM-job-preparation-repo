name: My first workflow

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main    
jobs:
  say-hello: 
    runs-on: ubuntu-latest
    steps:
      - name: print a message
        run: echo "Hello this is krishna chaitanya"
